/**
 * Centralized constants barrel export
 * Import from '@/constants' instead of deep importing individual files
 */

// Featured Products Configuration
export { FEATURED_PRODUCTS_CONFIG, ERROR_MESSAGES } from './featured-products'

// Messages (Validation, UI, Buttons)
export { VALIDATION_MESSAGES, UI_MESSAGES, BUTTON_LABELS } from './messages'

// App Configuration (API, UI, Routes)
export { API_CONFIG, UI_CONFIG, ROUTES } from './app-config'
